# Environment
.env
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Runtime outputs (regenerated by the daily agent)
output/daily/*.json
output/daily/*.md
output/daily/*.log
# Keep output/editions/ (published editions = training data + dedup source) — NOT gitignored

# Claude Code workspace (per-developer, not shared)
.claude/settings.local.json

# Per-install snapshot of the user's topics + include/exclude criteria,
# captured on first dashboard load. Used by Settings → Editorial's
# "Restore your initial setup" link. Local to each install.
.scout_install_snapshot.yaml

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
