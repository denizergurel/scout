feedparser>=6.0,<7
httpx>=0.27,<1
fastapi>=0.115,<1
uvicorn>=0.32,<1
pyyaml>=6.0,<7
trafilatura>=1.12,<2      # used to extract article content for manual URL ingest

# Optional LLM providers (install only if you switch LLM_PROVIDER away from claude):
#   pip install "google-generativeai>=0.8,<1"    # for LLM_PROVIDER=gemini
#   pip install "openai>=1.50,<2"                # for LLM_PROVIDER=openai
